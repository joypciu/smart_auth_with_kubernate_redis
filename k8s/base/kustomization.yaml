apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: smart-auth

resources:
  - namespace.yaml
  - postgres.yaml
  - redis.yaml
  - api.yaml
  - ingress.yaml
  - migration-job.yaml
  - network-policy.yaml

labels:
  - pairs:
      app.kubernetes.io/name: smart-auth-api
      app.kubernetes.io/part-of: smart-auth-platform
